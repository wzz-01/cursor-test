# 销售管理日简报 - 新智能体快速上手说明

> 把本文**整份**发给新智能体，作为任务说明书。  
> 目标：按锁定口径从 Doris 取数，并按样例句式输出三段正文（先不出 PDF，除非明确要求）。

---

## 0. 你要完成什么

为指定省区生成《销售管理日简报》中的核心三段文字：

1. **区域销售进度**（当月）
2. **业绩预警 / 市场问题**（近3个快照月）
3. **年度目标**（本财年至今）

原则：

- 数字必须来自 SQL，可追溯
- 禁止编造
- 落款日期 ≠ 取数快照日（见下文）
- 先出结果表 + 样例句式正文；用户确认后再谈 PDF

---

## 1. 数据源（先锁定）

- 引擎：Doris
- 主表：`market_db.dwd_sales_fiveyears`
- 连接：当前环境已配置（主机示例 `100.100.1.75:9030`，账号 `market`，默认库 `market_db`）
- 本任务**只许用这张销售事实表**做上述三段；拜访/费用/订单数等本表没有的模块，标「本版暂无」，不要猜其他表

### 关键字段

| 用途 | 字段 |
|------|------|
| 日期（快照） | `date_id` |
| 省区 | `province_region_name` / `province_region_code` |
| 销售组 | `sales_group_name` / `sales_group_code` |
| 城市 | `city` |
| 客户 | `cust_code` / `cust_name` |
| 渠道 | `cust_group_classify`（取值：直营、经销商、电商） |
| 日报销额（主金额） | `amt_yesterday` |
| 日报销量 | `qty_yesterday` |
| 预算（省区产品层级） | `ys_products` |
| 预估额（可选） | `amt_yg` |

### 主金额与渠道

- **所有销售额/同比/年累计主金额一律用 `amt_yesterday`**（方案1）
- **不要用 `amt_last_month` 做同比**
- 样例句式中的「经销」= `cust_group_classify='经销商'`
- 「直营」= `cust_group_classify='直营'`
- 「电商」可附注，不进截图主句也可

---

## 2. 日期规则（最容易错，必须先懂）

### 2.1 业务日 vs 取数日

本表 `date_id` **不是自然日流水**，业务日期被归到**当月 1 号快照**。

**默认规则（每天自动，不要等用户改日期）：**

- `D_biz` = **昨天**（按运行当日自然日减 1 天；可用 `CURRENT_DATE - INTERVAL 1 DAY`）
- 用户未特别指定日期时，**禁止写死某个历史日期**，每次执行都重新取昨天
- 仅当用户明确指定某日时，才覆盖默认的昨天

| 概念 | 含义 | 如何计算 |
|------|------|----------|
| 业务日 `D_biz` | 报告落款「截至日」 | **默认 = 昨天** |
| 取数快照日 `D0` | `D_biz` 所在月的 1 号 | `DATE_FORMAT(D_biz, '%Y-%m-01')` |
| 同比快照日 `D0_ly` | 去年同月 1 号 | `D0` 减一年 |

**正文写「截至{月}月{日}日」（用 D_biz），取数用 D0。禁止把落款写成月初 1 号。**

示例（仅帮助理解，不是固定参数）：若今天是 7月21日，则 `D_biz=7月20日`，`D0=7月1日`，`D0_ly=去年7月1日`。

### 2.2 财年

- 财年 = **当年 7/1 ~ 次年 6/30**
- 例：2025-07-01 ~ 2026-06-30 = **2025 财年**
- 例：2026-07-01 ~ 2027-06-30 = **2026 财年**
- 所有“年累计 / 年度目标”按财年，**禁止按自然年 1–12 月**
- 本期/去年财年缩写用 `fiscal_year_abbreviation`（如 F26/F25），随 `D_biz` 所属财年自动判断

### 2.3 三段时间窗（必须分开，禁止混用）

每次先由 `D_biz=昨天` 推导窗口（下面用“若昨天落在某年7月”举例）：

| 段落 | 时间窗 | 取数日期 |
|------|--------|----------|
| ① 区域销售进度 | 仅业务日所在月 | `D0` vs `D0_ly` |
| ② 业绩预警 | 近 3 个快照月 | `D0` 及往前两个月的 1 号 |
| ③ 年度目标 | 本财年起始 ~ 当前月快照 | 本期：本财年7/1 ~ `D0`；去年同期：上财年对应区间 |

说明：当 `D_biz` 落在财年首月时，③ 的年累计会接近当月快照，**这是正确现象**。

---

## 3. 计算公式

### 通用

- 金额展示：**原值 / 10000 → 万元**
- 达成率 = `SUM(amt_yesterday) / SUM(ys_products)`
- **预算不去重**（直接 SUM，不要 MAX/row grain 折预算）
- 同比增额 = 本期 - 去年同期
- 同比增速/增长率 = 增额 / 去年同期（除零 → NULL/算不出）

### ① 区域销售进度（当月）

在省区 + `D0` 上计算：

- 销售额、同比增额、达成率、增长率
- 省区达成率排名、增长率排名（在全部 `province_region_name` 中）
- 经销达成/增长、直营达成/增长
- 预估本月达成：优先评估 `amt_yg`；不合理则标「待补」
- 大单品达成率：无明确规则则括号留空
- Top3 / Bottom3：该省区 `sales_group_name` 按达成率

### ② 业绩预警（近3月）

- 连续三月下滑：城市 / 销售组 / 客户，严格 `M1 > M2 > M3`（各月 `SUM(amt_yesterday)`）
- 近三月达成率全国后 20% 销售组：全国销售组先算近3月达成率，取后 20%，再筛本省区
- 近3月达成率 = 近3月 `SUM(amt_yesterday)` / 近3月 `SUM(ys_products)`（预算仍不去重）

### ③ 年度目标（财年至今）

- 年累计销额（分子）= 本财年起始日 ~ `D0` 的 `SUM(amt_yesterday)`
- 去年同期年累计 = 上财年对应区间 `SUM(amt_yesterday)`
- 增额 / 同比增速：基于上述两个年累计（方案1）
- **累计进度分母 = 该省区本财年「全年预算」**，不是“财年至今/当月快照预算”
  - 全年预算取 `ys_products`，`SUM` 不去重
  - **已确认取法**：用字段 `fiscal_year_abbreviation` 圈定财年，再 `SUM(ys_products)`
    - 本期示例：`fiscal_year_abbreviation = 'F26'`（对应 2026 财年）
    - 去年示例：`fiscal_year_abbreviation = 'F25'`（对应 2025 财年）
  - 这是月度快照表，需把该财年下全部快照月预算相加（通常 12 个 `month_cn_name`），**不能**只用 `D0` 当月预算
  - 若后续月份预算已写入，必须纳入全年预算分母
- 累计进度 = 年累计销额 / 全年预算
- **同期实际进度**（已纠偏）：
  - 公式：去年同期年累计销额 ÷ 去年同期所有月份完整月销额
  - 分子：上财年对应区间 `SUM(amt_yesterday)`（与「去年同期年累计」相同）
  - 分母：上财年完整年度 `SUM(amt_last_month)`（完整月销额；按 `fiscal_year_abbreviation` 如 F25 全量）
  - **分母不是 `ys_products`，也不是只用去年截止当月**
  - 验证锚点：1638.68 ÷ 53114.59 ≈ **3.1%**（保留1位小数）；禁止再写 8.1% / 10.12%
- 在全部省区单位中：进度排名、增速排名（进度分母仍用各省区全年预算 `ys_products`）

### ③纠偏｜累计进度分母与同期实际进度（必读）

**累计进度错误示例（禁止再犯）：**

> 累计进度53.4% …（用当月/财年至今预算当分子分母，实为当月达成）

**同期实际进度错误示例（禁止再犯）：**

> 同期实际进度：8.1% / 10.12% …（误用去年全年预算 `ys_products` 作分母）

**正确对照：**

| 项 | 正确做法 | 错误做法 |
|----|----------|----------|
| 累计进度分子 | 本财年起始 ~ `D0` 的 `SUM(amt_yesterday)` | — |
| 累计进度分母 | Fxx 全年 `SUM(ys_products)` | 当月/至今预算 |
| 同期实际进度分子 | 去年同期年累计 `SUM(amt_yesterday)` | — |
| 同期实际进度分母 | 上财年全年 `SUM(amt_last_month)` | 上财年全年 `ys_products` |
| 同期实际进度锚点 | **3.1%** | 8.1% / 10.12% |

**与第一段的区别：**

- ① 达成率分母 = 当月预算 `ys_products`
- ③ 累计进度分母 = 全年预算 `ys_products`
- ③ 同期实际进度分母 = 上财年全年完整月销额 `amt_last_month`
---

## 4. 必须严格套用的输出句式

### 第一段（截图1）

> 截至X月X日，XX省区实现销售额XX万元，同比增加XX万元，达成率XX（其中大单品达成率：），增长率XX，达成率排名第X位，增长率排名第X位；经销达成XX，增长XX，直营达成XX，增长XX，结合当前进度及同期实际值，预估本月达成约XX。达成率前三分区/销售组依次为：……，后三为……（单位名称加达成）。

注意：`截至X月X日` 用 **业务日 D_biz**，不是 D0。

### 第二段（截图2）

有命中时：

> 省区内连续三个月下滑城市有……，销售组有……，客户有……，其中X、X销售组近三月达成率处于全国后20%，

无命中时（推荐）：

> 省区内无连续三个月下滑城市，无连续三个月下滑销售组，无连续三个月下滑客户，其中无销售组近三月达成率处于全国后20%，

不要写「城市有无，销售组有无」。

### 第三段（截图3）

> 年度目标：年累计销额XX万，增额XX万，累计进度XX（同期实际进度：XX），同比增速XX；在XX省区单位中，进度排名第X位，增速排名第X位。

同期实际进度按「去年同期年累计 ÷ 上财年全年 amt_last_month」填写，保留1位小数。

---

## 5. 已知可用核对锚点（豫南经销省区，D0=2026-07-01）

以下曾人工核对通过，可供新智能体自检（展示为万元）：

**当月（①）锚点：**

- 销售额约 **1641.82**
- 去年同月约 **1315.32**
- 同比增额约 **326.50**，增速约 **24.82%**
- 达成率约 **53.38%**（预算约 3075.86，不去重）
- Top3 达成：商周、三洛、信阳
- Bottom3 达成：南阳、驻马店、许平

**财年首日（③）锚点：**

- 年累计与当月销售额在财年首月场景下可一致
- 累计进度分母 = Fxx 全年 `ys_products`（验证版约 3.93% 量级，禁止 53%）
- **同期实际进度分母 = 上财年全年 `amt_last_month`**（验证：1638.68/53114.59≈**3.1%**；禁止再用全年预算算出的 8.1%/10.12%）

---

## 6. 推荐执行步骤（给新智能体）

按顺序做，每步输出给用户确认：

1. **连通与定位**：确认 `market_db.dwd_sales_fiveyears` 可查；`SHOW`/`DESC` 可选
2. **确认日期参数**：默认 `D_biz=昨天`，自动算 `D0`、`D0_ly`、财年（Fxx）、近3月；先回显这组日期再取数。用户未指定时不要写死日期。
3. **跑①当月指标** → 结果表 + 第一段样句
4. **跑②近3月预警** → 结果表 + 第二段样句
5. **跑③财年目标** → 结果表 + 第三段样句
6. 用户确认后，再按要求生成一页 PDF（未要求则停止）

默认参数：

- 省区：`豫南经销省区`（用户可改）
- `D_biz`：**每天自动取昨天**（不要写死）
- `D0` / `D0_ly` / 近3月 / 财年缩写：由 `D_biz` 自动推导

---

## 7. 禁止事项

- 不要把落款写成快照日 1 号
- **不要把业务日写死成某个历史日期**；默认每天取昨天，除非用户明确指定某日
- 不要用自然年做年累计
- 不要用 `amt_last_month` 做同比
- 不要对年度/当月预算做 MAX 去重（本任务明确不去重）
- **不要把「当月/财年至今预算」当成年度累计进度的分母**（见「③纠偏」；错误：1642/3075≈53.4%；正确：1642/59302≈2.8%）
- 全年预算必须用 `fiscal_year_abbreviation`（如 F26/F25）圈定后 `SUM(ys_products)`，不要只取 D0
- 不要把①的达成率与③的累计进度混为同一个分母口径
- 不要把①②③时间窗混在一段里
- 不要编造大单品、拜访、费用、订单数
- 查不到就写「算不出 + 原因」

---

## 8. 一键启动指令（复制即用）

```text
请阅读并严格遵守《销售管理日简报 - 新智能体快速上手说明》。

立即执行：
- 省区：豫南经销省区
- 业务日 D_biz：默认取昨天（CURRENT_DATE - 1天）；不要使用任何写死的历史日期
- 主表：market_db.dwd_sales_fiveyears
- 先根据昨天自动计算并回显：D_biz / D0 / D0_ly / 财年(Fxx) / 近3月
- 依次输出①②③：关键结果表 + 严格样例句式正文
- 正文落款用 D_biz（截至X月X日），取数用 D0；禁止落款写成1号
- 金额万元；当月达成用当月预算；**年度累计进度分母用省区全年预算**（fiscal_year_abbreviation 全量 SUM(ys_products)，非 D0/财年至今）
- 主金额 amt_yesterday；预算 SUM(ys_products) 不去重
- 不要 PDF，不要编造
```

> 说明：日常无需改日期。只有回溯历史某日时，用户才会额外指定 `D_biz`。
